const SECRET_KEY = "my-32-character-ultra-secure-and-ultra-long-secret";

const database = "conhub"
const username = "postgres"
const password = "postgres"
const host = "localhost"
const port = 5432
const dialect = "postgres"

export { 
    SECRET_KEY,
    database,
    username,
    password,
    host,
    port,
    dialect
 }